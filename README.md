# Multi-Zone-Enterprise-Network-with-Edge-Redundancy
WIP

## โครงสร้างของ Github Repository
```
/your-repo-name/
├── README.md               # อธิบายโปรเจกต์ (ที่คุณเพิ่งให้ผมเขียนให้)
├── Topology.pkt            # ไฟล์โปรเจกต์ Cisco Packet Tracer หลัก
├── /docs/                  # (แนะนำ) สำหรับเก็บไฟล์เสริมหรือคู่มือการติดตั้ง
│   ├── Network-Diagram.png # ไฟล์รูปภาพผังเครือข่ายที่ Export มาจาก Packet Tracer
│   └── IP-Addressing.pdf   # ตารางสรุปการแบ่ง IP ทั้งหมด
├── /configs/               # สำหรับเก็บไฟล์ CLI Configuration
│   ├── Edge-Firewall.txt
│   ├── Core-Switch.txt
│   ├── ISP-Router-1.txt
│   └── ISP-Router-2.txt
└── .gitignore              # ไฟล์ที่บอก Git ไม่ให้ track ไฟล์ที่ไม่จำเป็น (เช่น ไฟล์ชั่วคราว)
```
